package app.entity;

//Вводимо особливий тип класу record, що не завдає потреби створювати методи та конструктори (вони створюються автоматично)
public record CertainParametres(double parameterOne, double parameterTwo) {
}
